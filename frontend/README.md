# Frontend

Static frontend for the TwinMind-style meeting copilot.

## Run options

1. Open with VS Code Live Server from the `frontend/` folder.
2. Or run any simple static server from this folder.

By default the UI calls the backend at `http://localhost:8080/api`.
